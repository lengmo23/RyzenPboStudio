# CLAUDE.MD

## 项目说明

本项目是面向 AMD Ryzen 7000/9000 系列处理器的 Curve Optimizer（CO）负压测试与自动调整软件。主程序为 .NET 8 Windows Forms 应用，通过 SMU 接口读写 CO 参数，并使用 y-cruncher 执行压力测试。

涉及 CPU 电压、CO 参数、SMU 命令、压力测试、异常恢复或持久化逻辑时，应优先保证硬件安全和断电恢复可靠性。不得在没有用户明确要求的情况下扩大支持范围、改变负压步进策略或削弱现有安全保护。

## 沟通语言

- Agent 在大多数情况下应以中文为主要回复语言，并辅以必要的英文技术术语、命令、代码标识或简短英文对照。
- 中文表达应清晰直接；英文只用于帮助准确理解，不应无必要地把整段回复重复翻译一遍。
- 代码、API、类名、文件名、命令行参数及报错原文保持其原始英文形式，并在需要时补充中文解释。
- 若用户明确指定其他语言或输出格式，则遵循用户要求。

## 开发原则

- 修改前先阅读相关实现和项目配置，明确假设、影响范围及验收标准。
- 只实现用户要求的功能，保持改动小而集中，不顺手重构或格式化无关代码。
- 遵循现有 C#、WinForms 和项目命名风格，不为单次用途引入额外抽象或配置。
- 不覆盖、回退或删除用户已有的未提交改动。
- 若需求存在会影响硬件安全或功能行为的歧义，应先向用户确认。

## 主程序发布规范

完成主程序代码修改后，直接执行 Release 发布。**不使用单文件打包**，交付物是一个包含 EXE 及其依赖 DLL 的普通文件夹。所有交付内容必须统一输出到仓库根目录的 `bin` 文件夹，不得散落在项目目录、`publish` 文件夹或仓库根目录。

主程序默认发布命令：

```powershell
dotnet publish .\RyzenPboStudio\RyzenPboStudio.csproj -c Release -r win-x64 `
  --self-contained false `
  -p:DebugType=None -p:DebugSymbols=false `
  -o .\bin
```

发布要求：

- 保持目标框架为 `net8.0-windows`，但必须使用 `--self-contained false`；不要打包或附带 .NET 8 Runtime。
- **不要加 `-p:PublishSingleFile=true`**；`bin` 应是可直接分发的文件夹，EXE 与随行 DLL、`.runtimeconfig.json` 并列。
- 不得把 `bin\Release\net8.0-windows`、`net8.0` 或类似的目标框架目录作为交付物复制到 `bin`。
- 主程序的标准交付物为 `bin\AMD Ryzen PBO Studio.exe` 及同目录下的依赖文件；`bin` 中只保留当前这一份发布内容。
- y-cruncher 不内嵌进程序集，由发布流程自动拷到 `bin\tools\y-cruncher\`；不要改回内嵌。
- `bin\logs\`、`bin\profiles\` 是程序运行时产生的，不属于交付内容，发布前应清掉。
- 除非用户明确要求，不发布调试符号、中间构建文件或其他开发产物。
- 若发布失败，应修复导致发布失败的问题并重新发布；不得把未成功构建的文件作为交付物。

## 测试与用户验收

- Agent 完成主程序修改后，不执行 smoke test，不启动打包后的 EXE，也不代替用户运行真实的 CO 写入或 CPU 压力测试。
- 成功生成发布 EXE 后，告知用户 EXE 的路径、本次实现的功能以及建议重点验证的行为。
- 功能是否符合预期由用户实际运行并确认；收到用户反馈后，再针对反馈继续修改和重新发布。
- 除非用户明确要求，不额外执行会启动主程序、调用 SMU、修改 CO 参数或运行 y-cruncher 的测试。

## 完成标准

一次主程序开发任务仅在以下条件满足后才可交付：

1. 用户要求的代码改动已经完成，且未引入无关修改。
2. Release 单文件发布成功。
3. 最新 EXE 已生成到仓库根目录 `bin`。
4. 未执行 smoke test 或硬件压力测试。
5. 已向用户说明实现内容、EXE 路径和需要用户验证的项目。
