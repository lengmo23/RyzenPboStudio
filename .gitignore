# ── 闭源组件：绝不进入开源仓库 ────────────────────────────────────────────
# 授权/鉴权相关项目仅存在于本地工作区，开源版已完全移除对它们的依赖。
LicenseCore/
KeygenApp/
LicenseServer/
# 私钥文件：泄露即可伪造激活码
keygen-private-key.txt

# ── y-cruncher：不随仓库分发，由用户自行下载（见 README）────────────────────
y-cruncher*/

# ── 本地参考资料 / 调试工程 ────────────────────────────────────────────────
SMUDebugTool-*/
SMUDebugTool*.zip
MonitorTest/
legacy-runtime-backup-*/
runtime-backup-*/

# ── 发布包（由 bin\ 打包而来，走 GitHub Release 分发，不入库）──────────────
AMD-Ryzen-PBO-Studio-*.zip

# ── C# 构建产物 ───────────────────────────────────────────────────────────
bin/
obj/
*.user
publish/
*.sln.docstates

# ── 运行时产生的文件（在 exe 同目录）──────────────────────────────────────
logs/
profiles/
license.dat
trusted_time.dat
applied_offsets.ndjson
undervolt_state.json
undervolt_log.txt
final_offsets.txt
test_in_progress
.normal_shutdown
*.tmp
*.log

# ryzen-smu-cli：只保留构建实际需要的 inpoutx64.dll 与许可文件，
# 其余 .NET 运行时 DLL 与 CLI 本体不再使用（CO 读写已改走 ZenStates-Core）。
ryzen-smu-cli-0.1.3/*
!ryzen-smu-cli-0.1.3/inpoutx64.dll
!ryzen-smu-cli-0.1.3/InpOut.LICENSE.txt
!ryzen-smu-cli-0.1.3/LICENSE.txt

# ── 编辑器 / 系统噪声 ─────────────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
Desktop.ini

# 生成图标用的源图
ICON.png
ICON.PNG
icon.png
icon.PNG
